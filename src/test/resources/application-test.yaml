spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:ersync-${random.uuid};MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:

ersync:
  auth:
    jwt-secret-base64: dGVzdC1qd3Qtc2VjcmV0LWtleS0zMi1ieXRlcy1mb3ItdGVzdHM=
  invitation:
    expiry-scheduler-enabled: false
